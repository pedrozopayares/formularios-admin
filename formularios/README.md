En este directorio se encuentran los formularios que sirven de referencia para crear las taxonomías, CPT y grupo de Campos personalizados con el plugin ACF.

El código html de los formularios sirve para crear el layout que debe reproducirse con el plugin `acf-forms-frontend-creator`.